<?php

/**
 * Register the LAUDO Form Block and add the required custom fields
 * 
 */

// Register the custom blocks
register_block_type($plugin_root . 'block-files');

// Add the custom fields
// include_once($plugin_root.'/acf-fields.php');
